#include <stdio.h>
#include "platform.h"
#include <stdint.h>

int main() {

	uint8_t previous = 0;
	uint8_t current = 1;
	uint8_t summed = 0;
	uint8_t i = 0
	for(i <=15; i++){
		summed = previous + current;
		previous = current;
		current = summed;
		xil_printf(current);
	}


}
